#include <iostream>
#include <opencv2/opencv.hpp>
#include <opencv2/imgcodecs.hpp>
using namespace std;
using namespace cv;
/*
int main(int argc, char **argv) {
	Mat im = imread("Image.jpg", CV_LOAD_IMAGE_COLOR); // creates variable of type cv::Mat
	namedWindow("Hello");	// creates a window called hello
	imshow("Hello", im);	// shows image stored in im inside the window Hello

	cout << "Press 'q' to quit..." << endl;

	while (char(waitKey(1)) != 'q') {
	}		// wait until the q or Q key is pressed, then exit
	return 0;
}

*/
